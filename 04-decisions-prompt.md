# Prompt 3 · Apply the decisions (v1 → v2)

After the decision meeting, paste this with the v1 simulator file and your decision list (or screenshots of the marked-up screen).

---

Here is the v1 Quick List simulator `[simulator_v1.html]` and our decisions `[decisions.md or screenshots]`. Produce `simulator_v2.html` with the decisions applied:

1. Remove every line we dropped; move lines we moved; add lines we added (mark added lines with their source order set, or "addendum" if none).
2. Re-tag every remaining line **Include** and clear the Consider and Review tags for the populations we decided.
3. Keep the untouched populations exactly as in v1 and label them "not yet decided".
4. Keep group order and line order exactly as we specified; this file is the build spec.
5. Print a delta table: for each population × phase, lines in v1 → lines in v2, and groups in v1 → groups in v2.
6. Give me a one-page hand-off note for the build team: the final group structure per phase, each line's display name and matched Epic record, and any line that traces only to Foundation or to no reviewed order set (those need a pharmacy look).

---

**How we used it.** Adult lists went from 135 lines to 89, and from 22 groups to 14. The v2 file plus the hand-off note is what the pharmacy build team received.
